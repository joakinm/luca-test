export class Pregunta {
    titulo: string;
    publicacion: string;

    constructor (titulo: string, publicacion: string) {
        this.titulo = titulo;
        this.publicacion = publicacion;
    }
}